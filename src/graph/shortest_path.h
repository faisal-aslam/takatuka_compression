#ifndef SHORTEST_PATH_H
#define SHORTEST_PATH_H

#include "graph.h"
#include <stdbool.h>

// Public interface for shortest path calculation
int find_shortest_path_with_sequences(uint32_t target_node_id);

#endif